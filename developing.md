# Developing ailess.dev cli tool

First, install dependencies:

```bash
pip install .
```

Then you can run the cli tool like this:

```bash
python -m ailess init
```

or 

```bash
python -m ailess --help
```
